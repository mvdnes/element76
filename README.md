rust-kernel
===========

A kernel written in Rust

Build instructions
------------------

Obtain libcore:

- run `./downloadrustlibdir.sh` to download the i686 nightly libraries

Compile:

- `make`

Run:

- `make run`
